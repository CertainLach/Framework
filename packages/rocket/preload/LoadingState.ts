enum LoadingState {
    /**
     * On pending promise
     */
    LOADING,
    /**
     * On loaded data
     */
    LOADED,
    /**
     * On loading error
     */
    ERRORED
}
export default LoadingState;
